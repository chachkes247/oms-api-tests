version: '3.10'

services:
  fastapi:
    build: .
    ports:
      - "8000:80"
    depends_on:
      - mongo
    volumes:
      - ./app:/app
 
    environment:
      - MONGO_URI=mongodb://mongo:27017

  mongo:
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - ./data:/data/db
